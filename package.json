{
  "name": "planeta-azul",
  "version": "1.0.0",
  "description": "Asistente IA por WhatsApp con reservas para Planeta Azul (centro de buceo, Cabo de Palos)",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"node server/index.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "setup": "npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.109.0",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-rate-limit": "^7.2.0",
    "sql.js": "^1.12.0",
    "twilio": "^5.0.4"
  }
}
